import Hero from "./components/Hero";
import SocialProof from "./components/SocialProof";

const Home = () => {
  return (
    <main>
      <Hero />
      <SocialProof />
    </main>
  );
};

export default Home;
